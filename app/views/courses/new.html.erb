<% content_for :title, "수업 개설하기 | #{current_user.name}님의 대시보드" %>

<% content_for :main_class, "--fixed" %>

<div class="row">
  <%= render partial: "shared/sidebar", locals: { user: current_user, is_edit: false } %>

  <div class="col-lg-9 height--max">
    <header class="dashboard__header fixed--height flex--freeze">
      <h1>수업 개설하기</h1>
    </header>

    <section class="dashboard__content pb-80">
      <%= render partial: "form", locals: { user: current_user, course: @course }  %>
    </section>
  </div>
</div>